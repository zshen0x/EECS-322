(:go
 (:go
  0 0
  (rdi <- 5)
  (rsi <- 7)
  (call allocate 2) 
  ((mem rax 8) <- 11)
  ((mem rax 16) <- 13)
  (rdi <- rax)
  (call print 1)
  (rdi <- 7)
  (rsi <- rax)
  (call allocate 2) ; at the same array 
  (rdi <- rax)
  (call print 1)
  (return)))
