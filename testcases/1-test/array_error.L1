(:go 
 (:go
  0 0
  (rdi <- 301) ; rdi is first arg,
  (rsi <- 101) ; rsi is the second arg, 
  (call allocate 2)
  (rdi <- rax) ; rax is the result 
  (rsi <- 501)
  (call array-error 2)
  (return)))
