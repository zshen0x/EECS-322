(:main
  (:main
  0 4
  (rax <- 1)
  ((mem rsp 0) <- rax)
  (rax += 1)
  ((mem rsp 8) <- rax)
  (rax += 1)
  ((mem rsp 16) <- rax)
  ((mem rsp 24) <- rax)
  (rdi <- (mem rsp 0))
  (rdi <<= 1)
  (rdi += 1)
  (call print 1)
  (rdi <- (mem rsp 8))
  (rdi <<= 1)
  (rdi += 1)
  (call print 1)
  (rdi <- (mem rsp 16))
  (rdi <<= 1)
  (rdi += 1)
  (call print 1)
  (rdi <- (mem rsp 24))
  (rdi <<= 1)
  (rdi += 1)
  (call print 1)
  (return)))
